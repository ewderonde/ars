<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="author" content="ICT-DEAL">
    <title>Alcazar Roostersysteem</title>
    {# Als je javascript or css specifieke files wilt includen op een pagina, moet je de het block extenden met {{ parent() }} omdat je anders het hele blok overschrijft.
    {# Anders hoef je dit blok niet aan te spreken in andere templates.#}
    {# Voorbeeld:  <script src="{{ asset('bundles/js/main.js') }}"></script> voor JS of  <link href="{{ asset('bundles/css/style.css') }}" rel="stylesheet" />#}
    {# Dit path verwijst naar de /web folder. (/web/bundles/js of css )#}
    {% block styles %}
        {# include css files. #}
        {% include 'includes/header.html.twig' %}
        <link href="{{ asset('bundles/css/register.css') }}" rel="stylesheet" />
    {% endblock %}
</head>
<body>

<div class="container">
    <div class="row main">
        <div class="panel-heading">
            <div class="panel-title text-center">
                <h1 class="title">Alcazar Roostersysteem</h1>
                <hr />
            </div>
        </div>
        <div class="main-login main-center">
            <form class="form-horizontal" method="post" action="{{ path('login') }}">
                <input type="hidden" name="_target_path" value="dashboard_index" />

                <div class="form-group">
                    <label for="_username" class="cols-sm-2 control-label">Emailadres</label>
                    <div class="cols-sm-10">
                        <div class="input-group">
                            <span class="input-group-addon"><i class="fa fa-user fa" aria-hidden="true"></i></span>
                            <input type="email" id="_username" required="true" autofocus name="_username"{% if last_username %} value="{{ last_username }}"{% endif %} placeholder="Emailadres" class="form-control">
                        </div>
                    </div>
                </div>

                <div class="form-group">
                    <label for="_password" class="cols-sm-2 control-label">Wachtwoord</label>
                    <div class="cols-sm-10">
                        <div class="input-group">
                            <span class="input-group-addon"><i class="fa fa-lock fa-lg" aria-hidden="true"></i></span>
                            <input type="password" id="_password" required="true" name="_password" placeholder="Wachtwoord" class="form-control">
                        </div>
                    </div>
                </div>
                {% if error %}
                    <div class="form-group">
                        <div class="alert alert-danger">
                            <a href="#" class="close" data-dismiss="alert" aria-label="close" title="close">×</a>
                            {{ error.messageKey|trans(error.messageData, 'security') }}
                        </div>
                    </div>
                {% endif %}

                <div class="form-group ">
                    <button type="submit" class="btn btn-primary btn-lg btn-block login-button">Login</button>
                </div>
                <div class="login-register">
                    <a href="{{ path('register') }}">Registeren</a>
                </div>
            </form>
        </div>
    </div>
</div>
{% include 'includes/js.html.twig' %}
</body>
</html>
